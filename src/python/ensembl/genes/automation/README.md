# Genebuild Automation Utilities

This directory houses the scripts used in genebuild pipelines for automated workflow steps. 

Originally created to host the `pre_release_ftp.py` script that finds relevant files in the annotation directory and arranges them for copt to ftp. 
